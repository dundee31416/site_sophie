# Dev defaults. Copy to .env and override as needed.
POSTGRES_USER=lisons
POSTGRES_PASSWORD=lisons
POSTGRES_DB=lisons
POSTGRES_PORT=5432

BACKEND_PORT=8000
FRONTEND_PORT=5173

JWT_SECRET=dev-only-not-secret

ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin

# Optional: enables the Transcribe / Enhance / Restyle buttons in WorkEdit.
# Get from https://aistudio.google.com/apikey (paid tier required for image edit).
GEMINI_API_KEY=
