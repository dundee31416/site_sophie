# IDEs
.idea/
.vscode/
*.swp
*.swo

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Node
node_modules/
dist/
build/

# TypeScript build artifacts
*.tsbuildinfo
frontend/vite.config.js
frontend/vite.config.d.ts

# Storage volume target (host data, mounted in containers)
backend/data/

# OS
.DS_Store
Thumbs.db

# Claude
.claude/

# Env files — host-local config, not in git.
.env
.env.*
!.env.example
!.env.production.example

# Local asset folders — the real scans/photos/HTML belong on the prod host, not in git.
covers/
scans/
authors/
digital/

# Tool workspace output (Gemini enhance/extract scripts), not in git.
drawings/
enhanced/

# Local Gemini tool scripts/prompts (used by Claude agents, not the app)
tools/
